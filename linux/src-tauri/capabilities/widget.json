{"$schema":"../gen/schemas/linux-schema.json","identifier":"widget","description":"Capabilities for the always-on-top partner widget (label: widget). Minimal: drag it around, resize it via the corner grip, close it, focus it, and listen for harbor-widget-state events from the main window. No store, no sql, no window-state — the widget holds no persistent state, it renders what the main window emits.","windows":["widget"],"permissions":["core:default","core:window:allow-start-dragging","core:window:allow-start-resize-dragging","core:window:allow-close","core:window:allow-set-focus","core:event:default"]}
